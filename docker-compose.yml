services:
  api:
    build: ./server
    env_file: ./server/.env
    ports: ["8000:8000"]
  client:
    image: node:20-alpine
    working_dir: /app
    volumes: ["./client:/app"]
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports: ["5173:5173"]
    depends_on: [api]
